mysql:
  host: 'localhost'
  port: 9306
  user: 'root'
  password: 'admin'

clickhouse:
  host: 'localhost'
  port: 9123
  user: 'default'
  password: 'admin'

binlog_replicator:
  data_dir: '/tmp/binlog/'
  records_per_file: 100000

databases: 'test_replication'

target_databases:
  test_replication: test_replication_ch
